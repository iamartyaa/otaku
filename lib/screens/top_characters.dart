import 'package:flutter/material.dart';

class TopCharacters extends StatelessWidget {
  static const routeName = '/top-characters';

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}