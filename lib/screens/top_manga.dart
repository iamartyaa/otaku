import 'package:flutter/material.dart';

class TopManga extends StatelessWidget {
  static const routeName = '/top-manga';

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}